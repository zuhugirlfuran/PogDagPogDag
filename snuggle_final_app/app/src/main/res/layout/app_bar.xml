<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/app_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:elevation="4dp"
        android:paddingStart="27dp"
        android:paddingEnd="10dp"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="horizontal">

            <ImageView
                android:id="@+id/app_bar_iv_logo"
                android:layout_width="122dp"
                android:layout_height="68dp"
                android:src="@drawable/logo"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <ImageButton
                android:id="@+id/app_bar_ib_cart"
                android:layout_width="27dp"
                android:layout_height="27dp"
                android:layout_marginEnd="6dp"
                android:background="@color/basic_backgound"
                android:scaleType="centerCrop"
                android:src="@drawable/cart"
                app:layout_constraintBottom_toBottomOf="@+id/app_bar_iv_logo"
                app:layout_constraintEnd_toStartOf="@+id/app_bar_ib_notification"
                app:layout_constraintTop_toTopOf="parent" />

            <ImageButton
                android:id="@+id/app_bar_ib_notification"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:background="@color/basic_backgound"
                android:scaleType="centerCrop"
                android:src="@drawable/bell"
                app:layout_constraintBottom_toBottomOf="@+id/app_bar_iv_logo"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent" />
        </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.appcompat.widget.Toolbar>
</merge>
